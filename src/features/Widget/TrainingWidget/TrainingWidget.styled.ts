import { theme } from '@/app/Theme';
import styled, { css } from 'styled-components';

export const Training = styled.div`
  .dashboard-container {
    display: flex;
    gap: 10px;
    align-items: center;
  }
`;
